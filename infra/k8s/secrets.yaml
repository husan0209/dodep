apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: opus-casino
type: Opaque
stringData:
  # Database credentials (to be replaced with Vault)
  POSTGRES_USER: "opus_casino"
  POSTGRES_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  
  # JWT secrets
  JWT_SECRET_KEY: "CHANGE_ME_IN_PRODUCTION_USE_32_BYTES"
  JWT_REFRESH_SECRET: "CHANGE_ME_IN_PRODUCTION_USE_32_BYTES"
  
  # API keys (to be replaced with Vault)
  SUMSUB_API_KEY: "CHANGE_ME"
  STRIPE_SECRET_KEY: "CHANGE_ME"
  
  # Encryption keys
  ENCRYPTION_KEY: "CHANGE_ME_IN_PRODUCTION_USE_32_BYTES"
