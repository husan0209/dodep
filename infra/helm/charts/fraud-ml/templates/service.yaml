apiVersion: v1
kind: Service
metadata:
  name: {{ include "fraud-ml.fullname" . }}
  labels:
    {{- include "fraud-ml.labels" . | nindent 4 }}
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "{{ .Values.service.httpTargetPort }}"
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fraud-ml.selectorLabels" . | nindent 4 }}
